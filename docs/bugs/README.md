# 🐛 База знаний: Ошибки и Решения (Troubleshooting)

В этой директории хранятся отчеты о решенных проблемах, чтобы избежать их повторения в будущем.

### 📋 Структура отчета
Каждая запись должна содержать:
1. **Описание**: Текст ошибки и контекст возникновения.
2. **Решение**: Конкретные шаги или код, который исправил проблему.
3. **Предотвращение**: Что нужно изменить в процессах или тестах, чтобы ошибка не вернулась.

### 🔍 Поиск
Перед началом работы над новой ошибкой, попроси Copilot:
> *"Проверь папку docs/bugs на наличие похожих проблем."*

---
*Последнее обновление: 2026-02-09*

---

## Extracted short lessons (из удалённых/консолидированных файлов)

- **2026-02-09 — Vercel Image Optimization sinking quota:** критическая проблема — Image Optimization включена везде, что сжигает 5000/month лимит за день! Исправлено: `unoptimized: true` в next.config.ts и всех Image компонентах; все внешние изображения загружаются через наш `/api/image-proxy`.
- **2026-02-09 — Image-Proxy: aggressive HTTP caching + FANART_TV fallback:** исправлены критические ошибки кеширования ошибок на клиенте; добавлена поддержка FANART_TV как резервного источника постеров; rate limit 300 req/min.
- **2026-02-01 — Image Proxy + Redis caching:** реализован серверный `image-proxy` с кэшированием, retry и fallback; это исправило проблему с загрузкой постеров на мобильных устройствах.
- **2026-01 — Search fixes:** увеличен rate-limit для `/api/search` и заменено `include: { status }` на выборку `statusId`, что устранило гигантские IN-запросы.
- **2026-01 — Query optimization:** отложенная загрузка тегов (загрузка только для возвращаемых записей) и батчинг TMDB-запросов уменьшили количество JOIN/IN и сетевых вызовов.
- **2026-01 — Stats/detail perf:** параллелизация загрузки блоков профиля и использование `router.back()` для мгновенного возврата в профиль дали заметное улучшение UX.

---

```
